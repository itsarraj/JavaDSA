package LinkedList;

public class Node {
    int data;
    Node next;
    Node( int data ){
        this.data = data;
        next = null; //In Java Default address of any object is null so this statement is not required.
    }
}
